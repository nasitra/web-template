{
  "name": "",
  "version": "",
  "scripts": {
    "start": "browser-sync start --files='index.html, css/*.css, js/*.js' --server",
    "test": "browser-sync start --files='test/test.html, css/*.css, js/*.js, test/*.js' --server --startPath='test/test.html'"
  },
  "devDependencies": {
    "browser-sync": "*",
    "mocha": "*"
  },
  "private": true
}
